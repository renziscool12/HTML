# HTML

GIT COMMANDS

git init
git add .
git commit -m "first commit"
git remote add origin -link of github-
git branch -M main
git push commit -u origin

UPDATE

git add .
git commit -m "Describe what you changed"
git push
